<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="co.maeumi.prj.service.MemberMapper">


	<select id="memberSelectList"
		resultType="co.maeumi.prj.service.MemberVO">
		SELECT *
		FROM MEMBER
	</select>
	
	<select id="memberSelect" resultType="co.maeumi.prj.service.MemberVO">
		select * from member where m_email = #{m_email}
	</select>

	<select id="memberLogin"
		resultType="co.maeumi.prj.service.MemberVO">
		select * from member where m_email = #{m_email} and m_password = #{m_password}
	</select>

	<!-- 회원 닉네임 중복체크 -->
	<select id="memberNicknameCheck" resultType="boolean">
		SELECT CASE WHEN
		(COUNT(*) = 1) THEN 'TRUE' ELSE 'FALSE' END
		FROM MEMBER
		WHERE M_NICKNAME
		= #{m_nickname}
	</select>

	<!-- 회원 이메일 중복체크 -->
	<select id="memberEmailCheck" resultType="boolean">
		SELECT CASE WHEN
		(COUNT(*) =1 ) THEN 'TRUE' ELSE 'FALSE' END
		FROM MEMBER
		WHERE M_EMAIL =
		#{m_email}
	</select>

	<insert id="memberInsert"
		parameterType="co.maeumi.prj.service.MemberVO">
		INSERT INTO
		MEMBER(M_EMAIL, M_NICKNAME, M_TYPE, M_PHONE, M_PASSWORD)
		VALUES(#{m_email},#{m_nickname},#{m_type}, #{m_phone}, #{m_password} )
	</insert>


</mapper>